# KumuluzEE application configuration
kumuluzee:
  datasources:
    - jndi-name: jdbc/MoviesDataSource
      connection-url: jdbc:h2:tcp://localhost:8091/mem:movies
      username: 
      password: 
      pool:
        max-size: 20
  server:
    http:
      port: 8080

movies:
  counters:
    file-system:
      path: c:/temp/